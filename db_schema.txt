🔍 Inspecting Database: recruitement_deploy
============================================================
✅ Found 37 tables.

📦 TABLE: candidates
   - id (INTEGER)
   - user_id (INTEGER)
   - full_name (VARCHAR(150))
   - phone (VARCHAR(50))
   - dob (DATE)
   - address (VARCHAR(250))
   - gender (VARCHAR(50))
   - bio (TEXT)
   - title (VARCHAR(100))
   - location (VARCHAR(150))
   - nationality (VARCHAR(100))
   - id_number (VARCHAR(100))
   - linkedin (VARCHAR(250))
   - github (VARCHAR(250))
   - cv_url (VARCHAR(500))
   - cv_text (TEXT)
   - portfolio (VARCHAR(500))
   - cover_letter (TEXT)
   - profile_picture (VARCHAR(1024))
   - education (JSON)
   - skills (JSON)
   - work_experience (JSON)
   - certifications (JSON)
   - languages (JSON)
   - documents (JSON)
   - profile (JSON)
   - overall_interview_score (DOUBLE PRECISION)
   - cv_score (INTEGER)
   - dark_mode (BOOLEAN)
   - notifications_email (BOOLEAN)
   - notifications_push (BOOLEAN)
   - ethnicity (VARCHAR(50))
   - analyser_id (VARCHAR(255))
   - cv_analysis_status (VARCHAR(20))
   - cv_analysis_promoted_at (TIMESTAMP)
   - last_cv_analysis_id (VARCHAR(255))
------------------------------
📦 TABLE: cv_analyses
   - id (UUID)
   - record_id (UUID)
   - job_description (TEXT)
   - status (TEXT)
   - result (JSONB)
   - overall_score (DOUBLE PRECISION)
   - component_scores (JSONB)
   - warnings (JSONB)
   - started_at (TIMESTAMP)
   - finished_at (TIMESTAMP)
   - created_at (TIMESTAMP)
   - updated_at (TIMESTAMP)
------------------------------
📦 TABLE: cv_audit_logs
   - id (BIGINT)
   - entity_type (TEXT)
   - entity_id (UUID)
   - action (TEXT)
   - actor_id (UUID)
   - payload (JSONB)
   - ts (TIMESTAMP)
------------------------------
📦 TABLE: cv_records
   - id (UUID)
   - cv_text (TEXT)
   - status (TEXT)
   - created_at (TIMESTAMP)
   - updated_at (TIMESTAMP)
------------------------------
📦 TABLE: cv_resume_scores
   - id (UUID)
   - resume_id (UUID)
   - overall_score (DOUBLE PRECISION)
   - component_scores (JSONB)
   - explanation (JSONB)
   - created_at (TIMESTAMP)
   - updated_at (TIMESTAMP)
------------------------------
📦 TABLE: cv_resume_skills
   - id (INTEGER)
   - resume_id (UUID)
   - skill (TEXT)
   - canonical_skill (TEXT)
   - match_score (DOUBLE PRECISION)
   - evidence (JSONB)
------------------------------
📦 TABLE: cv_resumes
   - id (UUID)
   - uploaded_by (UUID)
   - filename (TEXT)
   - storage_key (TEXT)
   - content_type (TEXT)
   - size_bytes (BIGINT)
   - resume_text (TEXT)
   - status (TEXT)
   - created_at (TIMESTAMP)
   - updated_at (TIMESTAMP)
   - extraction_skills_count (INTEGER)
   - extraction_experience_count (INTEGER)
   - extraction_education_count (INTEGER)
   - extraction_personal_filled (INTEGER)
   - extraction_personal_total (INTEGER)
   - extraction_certifications_count (INTEGER)
   - extraction_overall_quality (DOUBLE PRECISION)
   - extraction_quality_level (VARCHAR(20))
------------------------------
📦 TABLE: user_presence
   - id (INTEGER)
   - user_id (INTEGER)
   - status (VARCHAR(20))
   - last_seen (TIMESTAMP)
   - socket_id (VARCHAR(100))
   - is_typing (BOOLEAN)
   - typing_in_thread (INTEGER)
------------------------------
📦 TABLE: users
   - id (INTEGER)
   - email (VARCHAR(150))
   - password (VARCHAR(200))
   - role (VARCHAR(50))
   - profile (JSON)
   - settings (JSON)
   - is_verified (BOOLEAN)
   - enrollment_completed (BOOLEAN)
   - dark_mode (BOOLEAN)
   - is_active (BOOLEAN)
   - created_at (TIMESTAMP)
   - first_login (BOOLEAN)
   - mfa_secret (VARCHAR(32))
   - mfa_enabled (BOOLEAN)
   - mfa_verified (BOOLEAN)
   - mfa_backup_codes (JSON)
   - last_login_at (TIMESTAMP)
   - username (VARCHAR(50))
------------------------------